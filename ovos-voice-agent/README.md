# OVOS Voice Agent (Legacy Sprint Artifacts)

This directory contains the Django stack and supporting assets.

For the active, supported platform, use:

- `ovos-voice-agent/AgentVoiceBoxEngine/README.md`
- `ovos-voice-agent/AgentVoiceBoxEngine/docs/LOCAL_DEVELOPMENT.md`

The current API surface is `/api/v2` and the WebSocket endpoints are under `/ws/v2`.
