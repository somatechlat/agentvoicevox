version: '3.8'
services:
  frontend:
    build:
      context: ./portal-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./portal-frontend:/app
      - /app/node_modules
    environment:
      VITE_API_URL: http://localhost:28000
    networks:
      - default
