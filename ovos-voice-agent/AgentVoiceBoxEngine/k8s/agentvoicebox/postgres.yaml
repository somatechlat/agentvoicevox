# ==========================================================================
# POSTGRESQL - AgentVoiceBox Isolated (1GB)
# ==========================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: agentvoicebox
data:
  POSTGRES_DB: "agentvoicebox"
  POSTGRES_USER: "avb_admin"
  POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: agentvoicebox
type: Opaque
stringData:
  POSTGRES_PASSWORD: "avb_secure_2024"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: agentvoicebox
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: agentvoicebox
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: postgres-config
            - secretRef:
                name: postgres-secret
          args:
            - "-c"
            - "max_connections=100" # Reduced for isolation
            - "-c"
            - "shared_buffers=128MB"
            - "-c"
            - "effective_cache_size=256MB"
            - "-c"
            - "work_mem=4MB"
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "100m"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "avb_admin", "-d", "agentvoicebox"]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "avb_admin", "-d", "agentvoicebox"]
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agentvoicebox
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
# NodePort for external access
apiVersion: v1
kind: Service
metadata:
  name: postgres-external
  namespace: agentvoicebox
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30014
  type: NodePort
