# Example environment variables (do NOT commit real secrets)
# Copy this file to .env and fill with real values locally.

# OpenAI-compatible API key (do not commit)
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-oss-20b

# TTS engine (kokoro/piper/espeak)
TTS_ENGINE=kokoro
KOKORO_VOICE=am_puck
KOKORO_SPEED=1.0

# Other settings
DEBUG=false
